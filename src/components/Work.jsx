export default function Work({timeline, company, position, location, link, description}) {
  return (
    <li>
      <div className="work">
        <h3>{timeline}</h3>
        
      </div>
    </li>
  );
}
